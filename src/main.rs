//! Otip - AI-powered cross-platform video player with lookahead content moderation
//!
//! This is the workspace root binary. The real application lives in `otip-app`.
//! Run with: `cargo run -p otip-app`

fn main() {
    eprintln!("This is the workspace root. Run the real app with:");
    eprintln!("  cargo run -p otip-app");
    std::process::exit(1);
}